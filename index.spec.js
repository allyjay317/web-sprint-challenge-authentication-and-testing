const db = require('./database/dbConfig')

describe('server.js', () => {
  describe('/api/auth', () => {
    describe('/api/auth/login', () => {

      it('should retrieve a user when passed a valid user', async () => {

      });
    });
    describe('/api/auth/register', () => {

    });
  });
  describe('/api/jokes', () => {

  });
});